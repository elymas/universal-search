{{- if .Values.embedder.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "universal-search.componentFullname" (dict "Release" .Release "Chart" .Chart "Values" .Values "component" "embedder") }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "universal-search.componentLabels" (dict "Chart" .Chart "Release" .Release "Values" .Values "component" "embedder") | nindent 4 }}
spec:
  type: {{ .Values.embedder.service.type }}
  ports:
    - name: http
      port: {{ .Values.embedder.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "universal-search.componentSelectorLabels" (dict "Chart" .Chart "Release" .Release "Values" .Values "component" "embedder") | nindent 4 }}
{{- end }}
