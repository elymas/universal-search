{{- if and .Values.usearch.mcp.enabled .Values.usearch.mcp.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "universal-search.serviceAccountName" (dict "Chart" .Chart "Release" .Release "Values" .Values "serviceAccount" .Values.usearch.mcp.serviceAccount "component" "mcp") }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "universal-search.componentLabels" (dict "Chart" .Chart "Release" .Release "Values" .Values "component" "mcp") | nindent 4 }}
  {{- with .Values.usearch.mcp.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
