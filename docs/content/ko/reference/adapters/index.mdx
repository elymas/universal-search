---
title: 어댑터 카탈로그
description: Universal Search 10개 프로덕션 어댑터 목록 (상태 배지 포함)
lastVerified: 2026-05-31
---

import { AdapterCatalog } from '../../../../components/AdapterCatalog'

# 어댑터 카탈로그

Universal Search는 10개의 프로덕션 어댑터를 포함합니다. 각 어댑터는 검색 소스를 래핑하고 공통 `Adapter` 인터페이스(`pkg/types.Adapter`)를 구현합니다.

아래 카테고리 필터로 목록을 좁힐 수 있습니다. 공유 오류 카테고리는 [오류 분류 체계](/ko/reference/adapters/errors) 페이지를 참고하세요.

<AdapterCatalog />

## 어댑터 라이프사이클

상태 배지는 어댑터의 라이프사이클 분류를 나타냅니다 (SPEC-DOC-002 D5):

| 배지 | 의미 |
|------|------|
| **stable** | 7일 롤링 성공률 ≥ 0.95 |
| **beta** | 7일 롤링 성공률 0.80–0.94 |
| **disabled** | 컴파일/플래그 게이트 스텁 — V1에서 실제 경로 없음 (현재: `x`) |
| **deprecated** | post-V1 어댑터 제거를 위한 예약 |

## 한국어 로케일 어댑터

한국어 콘텐츠에 최적화된 두 어댑터:

- **[Naver](/ko/reference/adapters/naver)** — `NAVER_CLIENT_ID` + `NAVER_CLIENT_SECRET` 필요; 한국어 UTF-8 쿼리를 그대로 전달
- **[Korean News](/ko/reference/adapters/koreanews)** — 공개 RSS + 선택적 KNC 사이드카; mecab-ko 중복 감지

인덱스 사이드 토크나이저 설정은 [한국어 로케일 설정](/ko/operators/korean-locale-setup)을 참고하세요.

## 관련

- [오류 분류 체계](/ko/reference/adapters/errors) — 공유 `Category` 참조
- [아키텍처 참조](/ko/reference/architecture) — 어댑터 인터페이스 계약
- [한국어 로케일 설정](/ko/operators/korean-locale-setup)
- [배포 가이드](/ko/operators/deployment-helm)
