# Framework-managed (moai-adk upstream)
.claude/
.moai/

# Build artifacts
web/.next/
web/node_modules/
node_modules/

# Lockfiles and generated
web/pnpm-lock.yaml
uv.lock
docs/licenses/
# Machine-generated docs — owned by gen-adapter-ref / gen-cli-reference; the
# drift-check CI jobs compare against the generators' exact output, so prettier
# must not reformat them (doing so caused gen-*-drift failures).
docs/content/en/reference/adapters/_generated/
docs/content/en/reference/cli/

# Test fixtures (intentionally malformed)
**/testdata/

# Helm templates: Go-templated YAML, not valid YAML for prettier to parse
charts/**/templates/
