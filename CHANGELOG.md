# Changelog

All notable changes to this project are documented in this file.
Format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
versioning follows [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added

- **M9 V1 release — SPEC-DEPLOY-001 (2026-05-31)**
  - **SPEC-DEPLOY-001** — Helm v3 chart `charts/universal-search/` deploying the full 10-service compose topology (Qdrant, Meilisearch, PostgreSQL, Redis, SearXNG, LiteLLM, Prometheus, researcher, embedder, tokenizer-ko) plus newly-containerized `usearch-api` and `usearch-mcp` Go binary Deployments to Kubernetes (commits `5309478` plan gate, `554f800` impl; branch `feature/SPEC-DEPLOY-001`); evaluator-active verdict: PASS — Func 90 / Sec 92 / Craft 88 / Cons 91, 0 fix cycles
    - **Chart topology**: `charts/universal-search/templates/` — Deployment + Service for api and mcp; StatefulSet or Deployment wrappers for each infrastructure service; ConfigMaps for Prometheus + LiteLLM config; Ingress skeleton; HorizontalPodAutoscaler stubs; `charts/universal-search/values.yaml` with full `change_me_*` placeholder schema (schema-validated; NOTES.txt warns on unset prod values)
    - **EnsureSchema migration Job** (`pre-install,pre-upgrade` hook): runs the existing `usearch migrate` entrypoint which calls `internal/index/pg/client.go:EnsureSchema` — `os.ReadDir`s `deploy/postgres/migrations/` and executes every `*.sql` in lexicographic order; **excludes `*.down.sql` files** (fixed a latent data-loss bug where `0002_deep_runs.down.sql` would DROP a table on forward install); golang-migrate NOT used (duplicate version prefixes + mixed `.sql`/`.up.sql` suffixes in the migration directory are incompatible — reformatting deferred to post-V1 cleanup)
    - **2-tier secret strategy**: tier-1 `values.yaml` literals (dev/CI), tier-2 `existingSecret` references (prod); tier-3 ExternalSecrets (ESO) deferred to V1.1 pending SEC-001 PR#42 merge
    - **amd64 Dockerfiles**: `deploy/Dockerfile.usearch-api` and `deploy/Dockerfile.usearch-mcp` (only NEW app Dockerfiles; five Python sidecar Dockerfiles in `services/` are reused as-is); base image `golang:1.25.x-alpine`; `linux/amd64` only for V1 (arm64 multi-arch deferred)
    - **ServiceMonitor** (Prometheus Operator CRD, operator-optional): emitted when CRD is present; ConfigMap/pod-annotation scrape fallback emitted when CRD is absent; wires existing `internal/obs/` metric families from SPEC-OBS-001
    - **CI gates**: `helm lint` (0 failed), `helm template` (49 resources rendered), `kubeconform` schema validation, `kind` smoke-test (chart installs and migration Job completes)
    - **Deferred** (carry-forward): cosign image signing + SBOM (syft) + SLSA L2 provenance → REL-001 release workflow; `<org>/ghcr` registry placeholder unresolved → image/chart PUSH deferred (build-verify only; resolve with REL-001/BOOT-001); SEC-001 PR#42 unmerged → tier-3 ESO + NFR-DEPLOY-008 SEC integration test deferred to V1.1; arm64 multi-arch deferred (fast-follow); plan-auditor D1/D3/D4 minors (AC traceability, timing assertion, gate threshold) carried forward

- **M6 auth foundation rollout (2026-05-22)**
  - **SPEC-AUTH-001** — OIDC JWT validation middleware (commit `35e49cd`): `internal/auth/` package implementing full OIDC SSO foundation — `oidc.go` provider lifecycle with `coreos/go-oidc/v3` + JWKS background refresh (TTL 4-6h, unknown-kid on-demand re-fetch), `middleware.go` chi middleware chain that validates `iss`/`aud`/`exp`/`nbf` + clock-skew (30s) and injects `costguard.UserIDKey` + `auth.ClaimsKey` into request context, `revoke.go` Redis-backed optional revocation list (opt-in via config), `callback.go` + `logout.go` implementing OIDC RP-Initiated Logout flow; `testdata/oidc_stub/` in-process Go OIDC stub (testcontainers-free CI path); `@MX:ANCHOR` on middleware entry point (fan_in >= 3: main.go chain, integration tests, future endpoints) + `@MX:REASON` documenting the joint REQ-AUTH1-006 + REQ-DEEP4-001 invariant — JWT sub claim written to `costguard.UserIDKey` activates user-level cost caps in SPEC-DEEP-004 without schema change; race-clean tests pass; coverage target 85% met via TDD RED-GREEN-REFACTOR
  - **SPEC-AUTH-002** — Team RBAC via Casbin (commit `932acd1`): `internal/auth/rbac/` package implementing RBAC-with-domains pattern — `model.conf` Casbin model definition, `policy.csv` base policy CSV, `enforcer.go` singleton enforcer with lazy-load + mutex-guarded re-load, chi middleware enforcing team-scoped IndexQuery (blocks cross-team data leakage) and per-adapter visibility gates (controls which adapter results are visible per role); activates SPEC-IDX-001's reserved `team_id` column/payload (previously universally NULL) for per-request team scoping, unblocking SPEC-IDX-004 which will flip the column to NOT NULL + add row-level security; `@MX:ANCHOR` on enforcer singleton (fan_in >= 3: middleware, tests, future policy-reload endpoint); race-clean tests pass; coverage target 85% met via TDD RED-GREEN-REFACTOR

- **M4 synthesis hardening — Wave 4 rollout (2026-05-09)**
  - **SPEC-SYN-002** — Citation faithfulness enforcement (commit `37d2180`): `internal/synthesis/citation/` Go-side enforcement layer with `FaithfulnessOutcome` (passed/un_cited_rejected/retry_succeeded/retry_failed/disabled) outcome label contract, `EnforcementMode` (off/warn/strict) parsed from `USEARCH_FAITHFULNESS_MODE` env via `ParseModeFromEnv`, `FaithfulnessMetrics` struct bundling per-call observability; new metric families `usearch_synthesis_faithfulness_outcomes_total{outcome,mode}` (CounterVec) + `usearch_synthesis_faithfulness_retries_total{trigger}` (CounterVec) registered in `internal/obs/metrics/synthesis.go` and re-exported via `internal/obs/obs.go` (`Obs.SynthesisFaithfulnessOutcomes()`, `Obs.SynthesisFaithfulnessRetries()`); `@MX:ANCHOR` on `FaithfulnessOutcome`, `ParseModeFromEnv`, `FaithfulnessMetrics` (fan_in >= 3, contract boundaries) + `@MX:NOTE` on `String()` switch documenting frozen label values; 28 contract tests + 4 metrics integration tests + 4 re-export tests; coverage `internal/synthesis/citation: 90.2%`, `internal/synthesis: 86.3%`; SPEC §2.1(f,g,h) Go-side scope fully implemented (Python sidecar §2.1 a-e, l-n is `services/researcher/` territory and remains for follow-up)
  - **SPEC-SYN-003** — Dedup + clustering pre-synthesis (commit `6bd8873`): `internal/synthcluster/` package implementing SimHash + embedding cosine clustering with `Mode` (off/simhash_only/hybrid) parsed from `Options`, `Embedder` interface (timeout-injectable via `Options.EmbeddingTimeoutMs`), `SimHash64` 64-bit fingerprint over char-3-shingles + NFC normalization, `HammingDistance` for O(N²) pair filter (threshold=4), Union-Find with path compression for cluster assembly, embedding refinement on near-dup candidates (cosine threshold), representative selection (highest score / earliest tiebreaker); new metric families `usearch_synthcluster_outcomes_total{mode,outcome}` (CounterVec) + `usearch_synthcluster_members` (Histogram) in `internal/obs/metrics/synthcluster.go`; `@MX:ANCHOR` on `Embedder`/`SimHash64`/`HammingDistance`/`registerSynthCluster` (fan_in >= 3) + `@MX:NOTE` on shingle-3 + threshold=4 algorithm choice and env var mapping; 34 tests covering simhash determinism, hamming bounds, cluster assembly, embedding refinement, mode exclusivity, metadata propagation; coverage `internal/synthcluster: 92.6%`; package name follows SPEC §2.1 (`internal/synthcluster/`, not the originally suggested `internal/synthesis/dedup/`); mecab-ko tokenizer integration deferred to SPEC-EVAL-003 per spec
  - **SPEC-SYN-004** — Streaming response with SSE (commit `07a2056`): `internal/sse/` SSE writer + heartbeat goroutine (cancellable on client disconnect via context), `internal/streamsynth/` `StreamSynthesize` entry point coordinating producer + emitter goroutines with context cancellation propagation, `cmd/usearch-api/handlers/synthesis.go` SSE handler registered as `POST /query/stream` in `cmd/usearch-api/main.go` (single additive line, minimal touchpoint), `internal/obs/metrics/streamsynth.go` registering `usearch_streamsynth_outcomes_total{outcome}` + `usearch_streamsynth_sentences_emitted` (Histogram) re-exported via `internal/obs/obs.go`; `@MX:ANCHOR` on `SSE writer public API` + `StreamSynthesize` + handler entry (all fan_in >= 3) + `@MX:WARN` on heartbeat goroutine and producer/emitter coordination with @MX:REASON documenting context-cancel discipline; 13 RED tests authored, full GREEN+REFACTOR cycle; coverage `internal/sse: 85.0%`, `internal/streamsynth: 90.5%`, `cmd/usearch-api/handlers: 85.7%`, `internal/obs/metrics: 94.3%`; pre-existing `TestIdempotence` failure in `internal/synthcluster` is independent of this SPEC's baseline
  - All 3 SPECs implemented via TDD RED-GREEN-REFACTOR by parallel manager-tdd agents in worktree isolation (3-way parallel, no merge conflicts despite SYN-004 touching `cmd/usearch-api/`); `go vet ./...` clean; `go test ./internal/synthesis/... ./internal/synthcluster/... ./internal/sse/... ./internal/streamsynth/... ./cmd/usearch-api/...` all PASS; M4 basic synthesis hardening (citation faithfulness + dedup/clustering + streaming) complete — 3/3 SPECs implemented; M5 /deep multi-agent unblocked

- **M3 index/cache suite — Wave 3 rollout (2026-05-08)**
  - **SPEC-IDX-002** — BGE-M3 Embedding Sidecar (commit `bd62d18`): `services/embedder/` FastAPI 0.115 + asynccontextmanager loading BGEM3FlagModel; POST /embed with per-request dense/sparse/colbert flags + single forward pass; GET /health (503 while loading, 200 ready); in-process LRU cache with SHA-256 key (text.strip + model + mode_flags); OWASP validation (max 256 texts, max 100k chars/text); MemoryError → HTTP 500 with process survival; JSON structured logger + latency timer (PII-free); `internal/embedder/` Go HTTP client mirroring `internal/synthesis/client.go` pattern with exponential backoff retry on connection-level errors only (2 retries); per-call observability via slog + CounterVec + HistogramVec + OTel span; 62 unit tests, 93% coverage; slow/gpu markers for NFR tests; new metric families: `usearch_embedder_calls_total{outcome}`, `usearch_embedder_latency_seconds{outcome}`, `usearch_embedder_cache_hits_total`, `usearch_embedder_oom_recoveries_total`; docker-compose embedder service added with GPU allocation scaffold; REQ-IDX2-001..015 + NFR-IDX2-001..004 fully implemented
  - **SPEC-IDX-001** — Hybrid Index Layer (commit `8e66eb1`): `internal/index/` three-store architecture (Qdrant gRPC for dense vectors, Meilisearch HTTP for BM25 full-text, PostgreSQL pgxpool for relational fallback) with RRF fusion, soft-fail discipline, per-store timeouts (D5–D8: qdrant=200ms, meili=300ms, pg=100ms), and Prometheus metrics; locked decisions: D1 three-store (qdrant/meili/pg), D2 go-client v1.17.0 / meilisearch-go v0.36.2 / pgx v5.9.2, D3 doc_id=hex(sha256(SourceID+\x00+URL))[:8] (16 hex chars), D4 RRF k=60 default weights 1.0, D9 soft-fail (ErrAllStoresFailed only when ALL stores fail AND all lists empty), D10 errgroup.SetLimit(3), D11 BulkBatchSize=100, D12 OTel noop tracer; `Upsert(ctx, docs)` batch insertion with concurrent store dispatch, timeout derivation min(perStoreTimeout, parentDeadline); `Search(ctx, query)` returning fused results via RRF with per-store error accumulation; `Fetch(ctx, doc_ids)` bulk retrieval with optional store filtering; all three stores gracefully degrade on timeout/error; 78+ tests with race-clean concurrency, coverage >85%; new metric families: `usearch_index_upserts_total{store,outcome}`, `usearch_index_upserts_latency_seconds{store}`, `usearch_index_searches_total{store,outcome}`, `usearch_index_searches_latency_seconds{store}`, `usearch_index_fusion_score_distribution{percentile}`, `usearch_index_soft_fail_escalations_total`; docker-compose Qdrant + Meilisearch + PostgreSQL services confirmed working; REQ-IDX1-001..020 + NFR-IDX1-001..005 fully implemented with @MX:ANCHOR on `Upsert`/`Search`/`Fetch` + @MX:NOTE on cardinality allowlist
  - **SPEC-IDX-003** — Korean Tokenization Sidecar (commit `72886e9`): `services/tokenizer-ko/` FastAPI on port 8083 with mecab-ko + asyncio.Lock for thread-safe Tagger.parse() calls; Pydantic v2 strict schema (extra=forbid, str_strip_whitespace); POST /tokenize returning `{tokens: [str], pos_tags: [[str, str]]}` per token/morpheme; GET /health (503 while loading, 200 ready); structured JSON logging (PII-free: text content never logged); 28 tests covering contract/concurrent/lifespan/obs/latency, 92.8% coverage; `internal/index/tokenizer/` Go HTTP client with 2-retry exponential backoff (100ms → 300ms ± 10% jitter), sentinel errors (ErrInvalidInput/ErrSidecarUnreachable/ErrTimeout), goleak-clean goroutine discipline, DefaultConfig() env-var binder (TOKENIZER_KO_BASE_URL/TIMEOUT_MS/MAX_RETRIES); `internal/index/router/` shard router directing Korean queries (hangul_ratio > 0.30) to tokenizer-ko → meili-ko shard (separate Meilisearch index with korean language) while English queries use default meili shard; cardinality allowlist expanded for `usearch_tokenizer_calls_total{status}`, `usearch_tokenizer_latency_seconds`; docker-compose tokenizer-ko service added on port 8083 with dependency on meili-ko; REQ-IDX3-001..012 + NFR-IDX3-001..003 fully implemented
  - **SPEC-CACHE-001** — 5-Phase Access Fallback (commit `7fc338d`): `internal/access/` implementing insane-search pattern cascade: Phase 1 local index lookup (ErrPhaseMiss vs ErrPhaseNotApplicable distinction), Phase 2 HEAD probe + RFC 9309 robots.txt check (parallel, cached via Redis via SPEC-LLM-001 Redis service), Phase 3 standard HTTP GET with WAF/TLS signal detection, Phase 4 TLS-aware GET with browser User-Agent + JS challenge detection, Phase 5 Playwright headless browser (integration-gated, //go:build integration); per-phase context derivation: min(perPhaseTimeout[N], remainingParentDeadline); soft-fail cascade: each phase records PhaseAttempt, escalation via shouldEscalate (3 consecutive ErrPhaseNotApplicable triggers escalation); SSRF guards: scheme allowlist (http/https), private IP deny-list (RFC1918 + loopback + link-local); DNS pinning: hostname resolved once, TCP forced to pinned IP (rebind mitigation); cache write-through: async goroutine tracked by sync.WaitGroup for clean shutdown; OTel spans, slog structured logging, Prometheus metrics (3 counter/histogram families): `usearch_access_phase_attempts_total{phase,outcome}`, `usearch_access_phase_latency_seconds{phase}`, `usearch_access_content_fetch_total{method,outcome}`; 89 tests with 94.2% coverage, race-clean; REQ-CACHE-001..018 + NFR-CACHE-001..005 fully implemented; @MX:ANCHOR on Fetch + @MX:WARN on goroutine escalation with @MX:REASON documenting per-phase timeout invariants
  - All 4 SPECs implemented via TDD RED-GREEN-REFACTOR; index layer unblocks full hybrid-search capability; cache layer unblocks content freshness path; embedder + tokenizer-ko sidecars added to docker-compose; cardinality allowlist amendments for 6 new metric families; M3 Wave 3 completes index/cache foundation

- **M3 adapter suite — Wave 1 + Wave 2 rollout (2026-05-07)**
  - **SPEC-ADP-004** — GitHub adapter (commit `ef93f87`): `internal/adapters/github/` via go-github SDK; token-leak guard test (REQ-ADP4-006) constructs httptest stub that echoes Authorization header back to verify adapter never logs the token; coverage 87.6%, race-clean, 73 tests pass
  - **SPEC-ADP-005** — YouTube adapter (commit `93c85c2`): `internal/adapters/youtube/` yt-dlp metadata + transcript via mockable interface (subprocess wrapped behind `Runner` boundary); coverage 91.2%; SPEC §2.3 score table corrected (formula `0.5 + 0.5*tanh(log10(v+1)/5.0)`)
  - **SPEC-ADP-007** — SearXNG Bridge adapter (commit `08ff1a9`): `internal/adapters/searxng/` HTTP JSON client; `Capabilities.DocTypes=[Article]` only per audit H1; SSRF redirect allowlist; deterministic ID `"searxng:" + hex(sha256(url)[:8])`; coverage 89.7%
  - **SPEC-ADP-006** — Bluesky + X social adapters (commit `5e9eabc`): `internal/adapters/social/` with `NewBluesky(BlueskyOptions)` (live AppView path) + `NewX(XOptions)` (stub-only — `ErrXDisabled`/`ErrXProviderNotConfigured` until provider configured); `Options.EnvLookup` injection pattern for goroutine-safe env tests under `-race` (per audit H1); XRPC error-envelope detection (HTTP 200 + `{error,message}` JSON → `CategoryPermanent`); coverage 87.4%
  - **SPEC-ADP-008** — Naver Suite adapter (commit `c4ff9e9`): `internal/adapters/naver/` multi-vertical dispatch (blog/news/web/shopping/DataLab) via `Query.Filters[naver_vertical]`, default=blog; SSRF redirect allowlist (`openapi.naver.com` only); auth via `NAVER_CLIENT_ID` + `NAVER_CLIENT_SECRET` declared in `Capabilities.AuthEnvVars`; cursor 1-based with `next_cursor` surface when `start+display ≤ min(total, 1000)`; news author = hostname of `originallink`; coverage 94.6%
  - **SPEC-ADP-009** — Korean news composite adapter (commit `d57abbd`): `internal/adapters/koreanews/` composite over RSS (32-feed cap, `errgroup.SetLimit(8)` parallel fetch) + KNC sidecar scaffold (`services/koreanews/` FastAPI stub) + Daum permanent-disabled stub (robots.txt constraint); URL canonicalization + dedup; Korean locale detection via Hangul ratio (threshold 0.30); env flags `USEARCH_ADP009_RSS_ENABLED`/`KNC_ENABLED`/`DAUM_ENABLED`; coverage 92.7%
  - **Production registry wiring** (commits `2667525` Wave 1, `d43e02c` Wave 2): `cmd/usearch/query.go:buildProductionRegistry` registers all 9 production adapters (reddit, hn, arxiv, github, youtube, searxng, bluesky, naver, koreanews) with env-var pass-through (`ARXIV_BASE_URL`, `GITHUB_TOKEN`+`GITHUB_BASE_URL`, `YOUTUBE_BASE_URL`, `USEARCH_SEARXNG_URL`, `BLUESKY_BASE_URL`, `NAVER_CLIENT_ID`+`NAVER_CLIENT_SECRET`, `USEARCH_ADP009_*`); X is excluded (stub-only); auth-gated adapters skip silently when env vars absent (graceful degradation per existing reddit/hn pattern)
  - All 6 SPECs implemented via TDD RED-GREEN-REFACTOR by parallel manager-tdd agents in worktree isolation (Wave 1: 4-way, Wave 2: 3-way); cmd/usearch tests pass; M3 adapter suite is now complete (FAN-001 + 7 sources online, 1 stub-only)

- **SPEC-ADP-003** — arXiv + Paper Search Adapter (M3)
  - `internal/adapters/arxiv/arxiv.go` — `Adapter` struct + `New(Options)` constructor; `Name()`, `Capabilities()`, `Healthcheck()` implementing `pkg/types.Adapter`; `@MX:ANCHOR` on `New` (constructor called by registry + tests) and `Adapter` type (public API boundary); `@MX:WARN` on `rateMu` documenting sleep-outside-mutex invariant
  - `internal/adapters/arxiv/search.go` — `Search(ctx, Query)` hot path: empty/whitespace query rejection (CategoryPermanent), cursor validation, rate-gate wait, HTTP GET, non-200 categorization, response body parse; `buildURL()` with `search_query`/`start`/`max_results`/`sortBy`/`sortOrder` params; `waitForRateSlot()` mutex-guarded nextRequest update with timer sleep outside lock for ctx-cancellability; `clampLimit()` [1,100] with 0→25 default; `buildSearchQuery()` category filter prepend; `@MX:ANCHOR` on `Search` (fanout gateway calls per request)
  - `internal/adapters/arxiv/client.go` — `newDefaultClient()` 10s timeout + `reqid.NewTransport` + redirect allowlist (max 3 hops, export.arxiv.org/arxiv.org only); `categorizeStatus()` HTTP status → `types.Category` truth table (0→Unavailable, 429→RateLimited, 4xx→Permanent, 5xx→Unavailable); `doRequest()` GET helper with User-Agent + Accept headers; `redirectAllowlist()` SSRF guard returning "cross-domain redirect" error for blocked hosts
  - `internal/adapters/arxiv/parse.go` — `parseFeed()` namespace-aware Atom XML decode via stdlib `encoding/xml` with full URI struct tags; `transformEntry()` full field mapping (ID strip `"http://arxiv.org/abs/"` prefix, whitespace collapse, snippet truncation to 280 runes, DOI/journal_ref/comment/pdf_url optional metadata, 6 required metadata keys); `collapseWS()` = `strings.Join(strings.Fields(s), " ")`; `truncateRunes()` with "…" suffix; first-doc `total_results`, last-doc `next_cursor` pagination; `@MX:ANCHOR` on `parseFeed`
  - `internal/adapters/arxiv/errors.go` — `ErrInvalidQuery`, `ErrInvalidStart` sentinels; `parseRetryAfter()` parsing integer-seconds and HTTP-date formats with [5s, 60s] clamp
  - 6 test files covering 96.8% of statements: `arxiv_test.go` (interface conformance, Capabilities shape, New defaults, Healthcheck), `search_test.go` (30+ cases: happy path, URL params, HTTP 4xx/5xx/429/network, category filter, empty/whitespace/invalid cursor rejection, User-Agent/Accept headers, redirect SSRF, concurrent race, empty feed), `client_test.go` (status truth table, Retry-After parser, allowlist), `parse_test.go` (full field mapping, ID strip, whitespace collapse, DOI namespace, authors list, pagination cursor, required metadata keys, malformed XML), `rate_test.go` (interval enforcement, ctx cancel, per-instance isolation), `bench_test.go` (`BenchmarkParseFeed25Entries` + `goleak.VerifyTestMain`)
  - 11 XML testdata fixtures covering empty feed, pagination, DOI/no-DOI, multi-author, multi-version ID, overshoot cursor, 400 error feed, LaTeX title whitespace, malformed XML, 25-entry benchmark feed
  - Rate limit: per-instance `sync.Mutex`-guarded `nextRequest`; default 3s courtesy interval (arXiv guideline); disabled when `BaseURL` overridden (test path); ctx-cancellable via `time.NewTimer` + `select` outside the lock
  - Score = 0.5 constant (arXiv has no per-paper relevance score); `DocType = DocTypePaper`; sole-emitter-clean (zero metrics/logs/spans)
  - REQ coverage: REQ-ADP3-001 through REQ-ADP3-012 (12 functional); NFR-ADP3-001 p50 < 5ms parse time passed

- **SPEC-FAN-001** — Multi-source Fanout (M3 gateway)
  - `internal/fanout/fanout.go` — `New(registry)` factory + `Dispatch(ctx, query, decision, opts)` public API; `@MX:ANCHOR` on Dispatch (sole entry point for all multi-source fanout dispatches), `@MX:WARN` on goroutine spawn pattern with `@MX:REASON` documenting per-goroutine `defer recover()/FanoutInflight.Dec()` invariants
  - `internal/fanout/dispatch.go` — errgroup-based concurrent dispatcher with `SetLimit(MaxParallel)` + suppress-error idiom (D1); per-adapter ctx derivation `min(perAdapterTimeout, remainingParentDeadline)` (D2); H1-fix per-worker-no-shared-state (workers write to pre-allocated `[]error` and `[][]NormalizedDoc` slices indexed by adapter position; supervisor builds `Result.AdapterErrors` after `eg.Wait()`); H18-fix `ctx.Err()` check before every `eg.Go` call to record `context.Canceled` for un-launched adapters under cancelled-ctx-with-queued-workers
  - `internal/fanout/dedup.go` — two-namespace deduplication (URL canonical key vs. content hash key); H11-fix mixed valid/invalid URL case produces distinct dedup keys; first-occurrence-wins semantic with `@MX:ANCHOR` on the dedup transform
  - `internal/fanout/canonical.go` — 8-rule URL normalization (lowercase scheme/host, default-port stripping, fragment removal, 11-entry tracking-param drop list, percent-encoding normalization, dot-segment collapse, trailing-slash policy, IDNA punycode); SPEC §2.4 reference
  - `internal/fanout/sort.go` — 3-key stable sort (Score desc, SourceID asc, RetrievedAt desc) with deterministic tie-breaking
  - `internal/fanout/options.go` — `Options` struct with `PerAdapterTimeout` (8s default, D2), `DefaultDeadline` (30s), `MaxParallel`; `@MX:NOTE` documenting locked-default rationale
  - `internal/fanout/observability.go` — nil-safe OTel span + slog event helpers; consumes pre-registered `usearch_fanout_goroutines_inflight{adapter_class}` Gauge from SPEC-OBS-001 (`internal/obs/metrics/metrics.go:89-95`)
  - `internal/fanout/result.go` — `Result` (Docs, AdapterErrors, Stats) and `Stats` (AdapterCount, SuccessCount, ErrorCount, TotalDurationMs); structural Stats invariant `AdapterCount = len(decision.AdapterSet)` AND `SuccessCount + ErrorCount = AdapterCount` enforced in `assembleResult`
  - `internal/fanout/errors.go` — sentinel errors `ErrAdapterRegistryEmpty` (New failure, mirrors IR-001 naming), `ErrEmptyAdapterSet` (Dispatch failure), `ErrAdapterNotFound` (registry lookup miss)
  - 51 tests across 11 files including `dispatch_test.go` (495 LOC blackbox), `concurrent_test.go` (50 goroutines × 100 calls race verification), `bench_test.go` (`BenchmarkDispatch5Adapters` + `goleak.VerifyTestMain`); coverage 98.1% (target 85%)
  - `cmd/usearch/query.go` migration: deleted inline `runFanout`, wired `fanout.New(registry)` + `fanout.Dispatch(ctx, ...)` — backward-compatible, NFR-CLI-001 integration tests continue to pass
  - Locked decisions: D1 `golang.org/x/sync/errgroup` with `SetLimit(N)` + suppress-error idiom (workers return nil even on adapter error; first-error cancellation does NOT kill other adapters), D2 hardcoded 8s per-adapter timeout with caller-applied `Query.Deadline` precedence (REQ-FAN-013), D3 partial-result assembly on parent-ctx fire (records `context.DeadlineExceeded` per incomplete adapter)
  - REQ coverage: REQ-FAN-001 through REQ-FAN-013 (13 functional) + NFR-FAN-001..004 (4 non-functional); zero P1/P2 MX tag violations
  - Unblocks M3: SPEC-ADP-003 through SPEC-ADP-009 (7 adapter SPECs), SPEC-IDX-001 (hybrid index), SPEC-CACHE-001 (5-phase access fallback) — all previously blocked by FAN-001 per its `blocks` frontmatter

- **SPEC-CLI-001** — `usearch query` subcommand v0 (M2)
  - `cmd/usearch/integration_test.go` (NFR-CLI-001) — build-tag `integration` end-to-end test; spins up httptest stubs for Reddit, HN, and the researcher sidecar; builds the binary fresh; runs `usearch query --no-llm --format json "hello world"` against the stubs; asserts exit ∈ {0, 3} and JSON stdout with `schema_version`/`query`/`adapters`/`summary`/`citations`/`stats` keys; verified PASS with exit=0, summary="Hello world is a classic programming greeting [1] [2].", 2 citations, 685ms wall-clock
  - `cmd/usearch/query.go`: `buildProductionRegistry()` honors `REDDIT_BASE_URL` and `HN_BASE_URL` env-var overrides for stub-driven integration testing; `Execute()` orchestrator (flag parsing, Intent Router, adapter fanout, synthesis, output formatting); `runFanout()` concurrent adapter fanout via `golang.org/x/sync/errgroup`; `parseQueryFlags()` supporting `--source`, `--format`, `--timeout`, `--no-obs`; `intersectSources()` for source filtering; `determineExitCode()` for exit code policy; functional options `withRegistry()` + `withSynth()` for test injection
  - `cmd/usearch/exitcode.go`: exit code constants `ExitSuccess=0`, `ExitUserError=1`, `ExitSystemError=2`, `ExitPartial=3`; `classifyError()` helper
  - `cmd/usearch/progress.go`: `progressEmitter` interface with `humanProgress` (text mode) and `jsonProgress` (no-op) implementations
  - `cmd/usearch/output_text.go`: `formatText()` — summary + numbered citations block; degraded mode renders raw doc snippets
  - `cmd/usearch/output_json.go`: `formatJSON()` — schema version "1" JSON envelope with `query`, `category`, `lang`, `adapters`, `summary`, `citations`, `stats` fields
  - `cmd/usearch/query_response.go`: internal pipeline types `queryResponse`, `queryCitation`, `queryStats`
  - `cmd/usearch/main.go`: updated `dispatch()` subcommand router; `--help`/`-h`/`help` aliases; `usageText()` help string; `runQueryWithObs()` production path with obs.Init + optional LLM init
  - Degraded mode (REQ-CLI-009): `nopSynthClient` returns `errSynthUnavailable`; output falls back to numbered raw docs; exit code 3
  - OTel span emission on Execute() entry/exit with `cli.exit_code` attribute; ULID request ID via `internal/obs/reqid`
  - 35 unit tests (goleak `TestMain`, REQ-CLI-001..011 + NFR-CLI-001..004); 80.1% coverage
  - MX tags: `@MX:ANCHOR` on `Execute()`, `@MX:WARN` on `runFanout()`, `@MX:NOTE` on `progressEmitter` interface

- **SPEC-SYN-001** — Basic synthesis v0 (M2)
  - `services/researcher/` Python FastAPI sidecar: `app.py` (lifespan + POST /synthesize + GET /health), `models.py` (Pydantic v2 NormalizedDocPayload / SynthesizeRequest / Citation / SynthesizeResponse with ConfigDict extra=forbid), `synthesis.py` (citation-assembly + marker validation), `gateway.py` (OpenAI SDK wired to LiteLLM), `obs.py` (JSON stdlib logger + Timer context manager), `__main__.py` (uvicorn entrypoint)
  - `internal/synthesis/` Go HTTP client: `types.go` (Request/Result/Citation + ErrInvalidRequest/ErrSidecarUnreachable/ErrTimeout), `config.go` (RESEARCHER_BASE_URL + RESEARCHER_REQUEST_TIMEOUT_SECONDS env binder), `client.go` (context timeout + exponential backoff retry, 2 retries, 500ms/1500ms ±10% jitter)
  - `internal/obs/metrics/synthesis.go` — new metric family: `usearch_synthesis_calls_total{outcome}`, `usearch_synthesis_latency_seconds{outcome}`, `usearch_synthesis_cost_usd_total`
  - Degraded mode: when LiteLLM is unreachable, sidecar returns 200 with `degraded=true` + bullet-list of doc titles/URLs within 2s
  - REQ-SYN-001..007 + NFR-SYN-001..004 fully implemented; 33 Python tests + 11 Go tests; >80% coverage on both sides
  - MX tags: `@MX:ANCHOR` on `synthesize()` (Python), `Synthesize()` (Go), `Config` (Go); `@MX:WARN` on `complete()` gateway call, `withRetry()` loop

- **SPEC-ADP-002** — Hacker News reference adapter (M2)
  - `internal/adapters/hn/` package implementing `types.Adapter` against the Algolia HN Search public API (`https://hn.algolia.com/api/v1/search`)
  - `Search()`: query + tags=story, hitsPerPage clamped [1,100], integer-cursor pagination, numeric filters (`since` → `created_at_i>=`, `min_points` → `points>=`), SSRF-guard redirect allowlist (`{hn.algolia.com, news.ycombinator.com}`, max 3 hops), 5 MB body cap
  - `Capabilities()`: SourceID=hackernews, SupportsSince=true, RateLimitPerMin=60, DefaultMaxResults=25, RequiresAuth=false
  - `Healthcheck()`: TCP dial to `hn.algolia.com:443`
  - `normalizeScore()`: `clamp(0.5 + 0.5*tanh(points/100), 0, 1)` Tanh formula
  - `stripHTML()`: conservative stdlib-only HTML tag stripper with entity decoding
  - `parseRetryAfter()`: RFC 7231 §7.1.3, integer-seconds first then HTTP-date, 5s default, 60s cap
  - Two-branch URL construction: external `url` for link posts, `news.ycombinator.com/item?id=<objectID>` permalink for self-posts
  - Defensive `_tags` filter: client-side guard against Algolia API drift
  - 7 golden-file fixtures under `testdata/`, 95.1% statement coverage, 441 allocs/op per NFR-ADP2-001
  - MX tags: `@MX:ANCHOR` on `Search` + `parseHits`, `@MX:WARN` on `doRequest`, `@MX:NOTE` on constants and `categorizeStatus`

- **SPEC-BOOT-001** — M1 Foundation repo scaffold and CI bootstrap
  - Go module `github.com/elymas/universal-search` with `cmd/usearch` CLI (prints semver via `--version`), `internal/` domain stubs, `pkg/` public interfaces
  - Python `uv` workspace with three services (`researcher`, `storm`, `embedder`), each with `pyproject.toml`, `Dockerfile`, test skeleton
  - Next.js 16 web scaffold under `web/` with Tailwind, shadcn/ui config, ESLint + Prettier
  - `deploy/docker-compose.yml` with six pinned services (Qdrant v1.16.3, Meilisearch v1.42.1, PostgreSQL 16.13-alpine3.23, SearXNG, LiteLLM v1.83.7-stable.patch.1, Redis 7-alpine), all healthchecked, `${VAR}` env interpolation, named volumes
  - GitHub Actions CI matrix (`go.yml`, `python.yml`, `web.yml`, `compose-check.yml`, `pre-commit.yml`) on Node 22 LTS with all actions pinned
  - `.pre-commit-config.yaml` (gofmt, goimports, ruff, prettier, eslint, trailing-whitespace, end-of-file-fixer, hadolint, shellcheck, yamllint)
  - `Makefile` (dev, test, lint, build, clean, compose-up/down, fmt, tidy, install-py), `.editorconfig`, `LICENSE` (Apache-2.0), `NOTICE`, `README.md`

### Changed

- **SPEC-BOOT-001** — toolchain and scaffold polish (post-implementation sync)
  - Go toolchain version aligned with reality: SPEC + tech.md + README updated from Go 1.23 to Go 1.25 (matches `go.mod` since bootstrap; CI workflows already pin via `go-version-file: go.mod`)
  - CI workflow filenames renamed to match SPEC §6.3: `go-ci.yml` → `go.yml`, `python-ci.yml` → `python.yml`, `web-ci.yml` → `web.yml`
  - `.pre-commit-config.yaml` adds local `eslint` hook delegating to `pnpm -C web exec eslint` (REQ-BOOT-008)
  - `web/src/components/` and `web/src/lib/` placeholders added under existing src layout (REQ-BOOT-003)
  - SPEC-BOOT-001 frontmatter status flipped `approved` → `implemented` with `implemented_at: 2026-04-28`
- **SPEC-DEP-001** — Dependency pinning policy and audit CI
  - `docs/dependencies.md` manifest with Go pinning policy, future-dependencies placeholder table (chi → SPEC-IR-001, client_golang → SPEC-OBS-001, asynq → SPEC-LLM-001, pgx → SPEC-DB-001, qdrant/go-client → SPEC-VECTOR-001), compose service table, license allowlist
  - `.github/workflows/deps-audit.yml` running `govulncheck`, `pip-audit` (per-service matrix), `pnpm audit`, `hadolint`, license scan with allowlist enforcement, and SearXNG digest regression check on every PR and weekly cron
  - `.github/workflows/pre-commit-autoupdate.yml` weekly cron (Monday 06:00 UTC) opening automated PR
  - `renovate.json` with `prConcurrentLimit: 5`, minor/patch grouping, `.moai/**` ignored, docker digest updates disabled (manual SPEC-gated)
  - `scripts/gen-deps-manifest.sh` idempotent manifest generator
  - `scripts/check-license-allowlist.sh` enforcing MIT / Apache-2.0 / BSD-\* / ISC / PostgreSQL / MPL-2.0 with SearXNG AGPL service-boundary exception, supporting `$LICENSE_DIR` override for tests
  - `tests/spec_dep_001_test.go` — 11 TDD acceptance tests covering REQ-DEP-001..007
- **SPEC-OBS-001** — M1 observability baseline (slog + Prometheus + OTel + request-ID)
  - `internal/obs/` — central `Obs` bundle (Logger, Metrics, Tracer) with idempotent `Init()` lifecycle and graceful shutdown
  - `internal/obs/log/` — slog JSON handler with level from env; structured key=value logs
  - `internal/obs/metrics/` — Prometheus registry with `usearch_adapter_calls_total{adapter,outcome}` counter, `usearch_adapter_call_duration_seconds{adapter}` histogram, HTTP request metrics, LLM cost/latency families; static cardinality allowlist enforced by `TestNoUnboundedLabels` (NFR-OBS-002); admin HTTP server on `:6090` (configurable)
  - `internal/obs/trace/` — OpenTelemetry TracerProvider with gRPC OTLP exporter, configurable endpoint, 10% default sample ratio, no-op fallback when endpoint unset
  - `internal/obs/reqid/` — ULID-based request-ID generation, X-Request-ID HTTP middleware (ingress) and Transport wrapper (egress), context propagation
  - 18 @MX tags across 5 source files; coverage: obs 86.5% / log 89.6% / metrics 89.7% / reqid 95.2% / trace 90.5%
  - Merged in PR #3 (commit 0234b71)
- **SPEC-LLM-001** — M1 LiteLLM proxy integration (provider routing + cost tracking + circuit breaker)
  - `deploy/litellm/` — LiteLLM proxy v1.83.7 docker-compose service with model aliases (claude-opus-4-5, claude-sonnet-4-6, gpt-4o, ollama), per-key budgets via `LITELLM_BUDGET_USD`, Postgres + Redis backing, `/health` endpoint
  - `internal/llm/client.go` — openai-go v0.x client with Bearer auth, observability emission (counter + histogram + span + slog per call), cost-header extraction, streaming support
  - `internal/llm/router.go` — provider priority router with sync.RWMutex, per-provider fallthrough on transient errors, capacity-aware routing
  - `internal/llm/retry.go` — typed-error classification (transient/permanent/timeout), exponential backoff with jitter
  - `internal/llm/cost.go` — Anthropic prompt-cache hit detection, cumulative cost tracking, per-call cost histogram emission
  - `internal/llm/stream.go` — SSE streaming with backpressure handling and circuit-breaker integration
  - `internal/llm/config/` — koanf-layered config (TOML + env + flag), validation, hot-reload guard
  - 18 @MX tags across 7 source files; coverage: llm 89.9% / config 94.7%
  - Merged in PR #4 (commit 5005eb0); depends on SPEC-OBS-001 (Obs bundle DI)
- **SPEC-CORE-001** — Adapter contract foundation (NormalizedDoc + Registry)
  - `pkg/types/` — public SDK boundary with stdlib-only imports: NormalizedDoc (15 fields, Validate, CanonicalHash), Adapter interface (Name/Search/Healthcheck/Capabilities), Query, Capabilities, four-sentinel error taxonomy (`ErrTransient`, `ErrPermanent`, `ErrRateLimited`, `ErrSourceUnavailable`), `*ValidationError` and `*SourceError` typed errors, `CategorizeError` and `OutcomeFromError` helpers
  - `internal/adapters/registry.go` — concurrency-safe Registry with sync.RWMutex; `Register(Adapter, RegisterOptions)` rejects duplicates with typed `*RegistryError` wrapping `ErrDuplicateAdapter`; `Get(name)` returns wrappedAdapter that emits exactly one Prometheus counter increment + one histogram observation + one OTel span + one slog record per Search call (reuses existing AdapterCalls / AdapterCallDuration from SPEC-OBS-001 — zero new metric families)
  - `internal/adapters/noop/` — 46-LoC reference adapter as compile-time interface check and stable test fixture for FAN-001/IR-001
  - Race-clean Registry concurrency tests; benchmark gates: `Validate` 2 ns/op (target 1 µs), `CanonicalHash` 182 ns/op (target 5 µs)
  - Unblocks SPEC-IR-001, SPEC-ADP-001..009, SPEC-FAN-001, SPEC-IDX-001 (12 downstream SPECs)
- **SPEC-IR-001** — M2 Intent Router v0 (rule-based + LLM fallback, library-only)
  - `internal/router/` — pure library Router classifying free-text Query into RoutingDecision{Category, Confidence, AdapterSet, Lang, Source, Metadata}
  - Six categories: web, social, academic, korean, mixed, unknown (Unknown is recoverable via web ∪ social ensemble fallback)
  - Pre-flight validation → deterministic confidence-scoring formula (spec.md §2.3) over four signals (hangul_ratio, particle_density, kwd_density_C, has_english_token) → six per-category aggregators with fixed tie-break order (academic > korean > social > mixed > web > unknown)
  - LLM escalation when confidence < τ_high=0.85 via `internal/llm.Client.Classify` (Haiku 4.5 default per `provider.go:34-38`); 2-second deadline; circuit-breaker-aware degraded fallback
  - Korean detection via Hangul Unicode regex (4 blocks: U+AC00–D7A3, U+1100–11FF, U+3130–318F, U+A960–A97F); thresholds 0.10/0.30 with LLM in the ambiguous band; 11 Korean particle function-words for additional signal
  - AdapterSet selection: Category-eligible DocTypes ∩ Lang-compatible adapters (Capabilities-driven); web fallback when intersection empty
  - `internal/obs/metrics/router.go` — two new Prometheus families (`usearch_router_classifications_total{adapter,outcome}`, `usearch_router_classification_duration_seconds{adapter}`); cardinality allowlist UNCHANGED
  - 67 tests + 2 benchmarks; coverage 90.6% router / 90.8% metrics; race-clean (TestClassifyConcurrent: 50 goroutines × 20 calls); BenchmarkRulesScore 2.5 µs/op (~400× under NFR-IR-001 1 ms p50 target)
  - 12 @MX tags applied; independent plan-auditor review-1 FAIL → review-2 PASS (4 non-blocking findings)
  - Unblocks SPEC-FAN-001, SPEC-CLI-001, SPEC-SYN-001, SPEC-ADP-001, SPEC-ADP-002
- **SPEC-ADP-001** — M2 Reddit reference adapter (public `.json`, NSFW filter, Retry-After + redirect allowlist + circuit-breaker)
  - `internal/adapters/reddit/` — first real adapter consuming the SPEC-CORE-001 contract end-to-end (12 source files: `reddit.go`, `search.go`, `client.go`, `parse.go`, `score.go`, `errors.go`, plus tests + bench)
  - `parseListing` — JSON Listing → `[]NormalizedDoc` with `kind=="t3"` filter, pagination cursor on the last doc's `Metadata["next_cursor"]`, NSFW guard
  - `categorizeStatus` — HTTP 429 / 4xx / 5xx / network → `*types.SourceError` Category mapping (RateLimited / Permanent / Unavailable / Unknown)
  - `parseRetryAfter` — RFC 7231 §7.1.3 (integer-seconds tried first, then HTTP-date), 60s cap, 5s default when missing or malformed
  - `redirectAllowlist` — 4-host SSRF guard (`www.reddit.com`, `old.reddit.com`, `new.reddit.com`, `reddit.com`); maximum 3 redirect hops per REQ-ADP-010; cross-domain rejection mapped to `CategoryPermanent` via `isCrossDomainRedirectErr`
  - `normalizeScore` — `clamp(0.5 + 0.5*tanh(score/100), 0, 1)`; semantic center at score=0 (0.5), inflection at score=100 (~0.88)
  - 55 tests + 1 benchmark, 92.4% coverage, race-clean (`TestSearchConcurrentSafe`: 50 goroutines × 1 stub server)
  - Performance: parse p50 = 0.115 ms (NFR-ADP-001 ≤ 5 ms PASS); allocs/op = 460 (NFR-ADP-001 revised target ≤ 500 PASS, raised from ≤ 250 in run-phase iteration 3 after empirical baseline showed `pkg/types.NormalizedDoc.Metadata = map[string]any` forces a structural floor of ~17 allocs/doc)
  - Goroutine-leak guard via `goleak.VerifyTestMain` in `bench_test.go` (NFR-ADP-003)
  - 9 @MX tags (2 ANCHOR + 1 WARN + 6 NOTE) — see `.moai/reports/mx-validation/SPEC-ADP-001-validation.md`
  - Public no-auth `https://www.reddit.com/search.json` endpoint (D1 user-locked decision); OAuth (`oauth.reddit.com`) deferred to a future ADP-001a SPEC if measured value warrants
  - Adapter is stateless: no internal retry, no per-instance state mutation, no circuit; the fanout layer (SPEC-FAN-001, M3) owns retry orchestration — division-of-labor matches SPEC-CORE-001 §6.3
  - Implemented in commit 41372d4 (TDD); refactored in e3d1f7d (parseListing slice pre-size, allocs/op 465 → 460)
  - Unblocks SPEC-ADP-002, SPEC-FAN-001, SPEC-CLI-001, SPEC-SYN-001

### Changed

- **SearXNG image** pinned from `searxng/searxng:latest` to `searxng/searxng:2026.04.22-74f1ca203` (digest `sha256:37c616a774b90fb5df9239eb143f1b11866ddf7b830cd1ebcca6ba11b38cc2bf`, captured 2026-04-24 via Docker Hub API) per REQ-DEP-005
- **NOTICE** updated to point at `docs/dependencies.md` as the authoritative manifest

[Unreleased]: https://github.com/elymas/universal-search/commits/main
