name: Web CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  web:
    name: Next.js typecheck + lint + build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: latest

      - name: Set up Node 22
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "pnpm"
          cache-dependency-path: web/pnpm-lock.yaml

      # Use --no-frozen-lockfile until pnpm-lock.yaml is generated by `pnpm install`.
      # SPEC-DEP-001 will tighten this to --frozen-lockfile once the lockfile is committed.
      - name: Install dependencies
        run: pnpm --dir web install --no-frozen-lockfile --config.onlyBuiltDependencies="sharp,unrs-resolver"

      - name: Type check
        run: pnpm --dir web typecheck

      - name: Lint
        run: pnpm --dir web lint

      - name: Build
        run: pnpm --dir web build
